<import src="../../components/helpdata/helpdata.wxml"/>

<view class='container'>
  <view wx:for="{{helpList}}" wx:key="{{objectid}}">
    <template is="helpdata" data="{{...item}}"/>
  </view>
  
</view>